apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: sm-api-developer-portal-pod-kill
  namespace: sm-prod
spec:
  action: pod-kill
  mode: one
  selector:
    namespaces:
      - sm-prod
    labelSelectors:
      app.kubernetes.io/instance: sm-api-developer-portal
      app.kubernetes.io/name: sm-api-developer-portal
  scheduler:
    cron: '@every 30m'
