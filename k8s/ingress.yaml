apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sm-api-developer-portal
  namespace: sm-platform
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "1m"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - sm-api-developer-portal.sm-platform.example.com
      secretName: sm-api-developer-portal-tls
  rules:
    - host: sm-api-developer-portal.sm-platform.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: sm-api-developer-portal
                port:
                  number: 8300
